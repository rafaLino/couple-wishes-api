version: "2"
sql:
  - engine: "postgresql"
    queries: "infrastructure/sql/query.sql"
    schema: "infrastructure/sql/schema.sql"
    gen:
      go:
        package: "db"
        sql_package: "pgx/v5"
        out: "infrastructure/db"